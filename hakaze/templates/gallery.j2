<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>{{ gallery_title }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="/static/styles.css" rel="stylesheet">
</head>

<body>
    <div style="display:flex;">
        <button class="button-page" onclick="location.href = '/g/{{dirname}}?p={{current_chapter - 1}}'">&lt;</button>
        <div id="pages-container">
            {% for number, page in pages %}
                <div class="cover-container" onclick="location.href = '/p/{{dirname}}/{{number + 1}}'">
                    <div class="cover-image">
                        <img src="{{ page }}" />
                    </div>
                </div>
            {% endfor %}
        </div>
        <button class="button-page button-page-right"
        onclick="location.href = '/g/{{dirname}}?p={{current_chapter + 1}}'">&gt;</button>
    </div>
</body>

</html>
